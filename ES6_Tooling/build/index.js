"use strict";

var x = 1;

/*
use babel
* package.json
"scripts": {
    "babel":"babel --presets env index.js -o build/index.js"
  }

  -o -> output
  the folder biuld must be created inside the node folder
*/
