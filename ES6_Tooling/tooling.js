/*
    Transpiler = translator and compiler : convets our modern code into code that all browsers can understand
    Example: Babel

    Bundler = responsible fro combinig all our javascripts files into a single file = bundle
    Example: Webpack


    ------------------------------
    Babel Instalation:
    * Go to a directory
    npm init --yes    this creates a package.json
    *install babel
    npm i babel-cli@6.26.0 babel-core@6.26.0 babel-preset-env@1.6.1 --save-dev

    * babel-cli -> command line interface 
    * babel-core -> all the logic for traspiring code is here
    * babel-preset -> plugings (new features)
    * --save-dev to save just on the development machine
*/